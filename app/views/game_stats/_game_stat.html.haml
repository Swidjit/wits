.game-stat
  %div
    =link_to game_stat.user.username, profile_path(game_stat.user.username)
  %div.skinny #{(game_stat.pct*100).round(1)}%
  %div.skinny #{(game_stat.score_avg).round(1)}
  %div.skinny=game_stat.score