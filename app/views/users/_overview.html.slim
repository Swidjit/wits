.user
	.user-image
		=link_to profile_path(user.username) do 
			=image_tag('profile.svg', :size=> '40x40')
	.user-name
		=link_to "#{user.username} (hey)", profile_path(user.username)
			
	.user-info 
		|
			num posts:
			#{user.posts.size}
			|
			creativity score:  #{user.score/100} 

